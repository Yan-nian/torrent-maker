# Torrent Maker 依赖包
# v1.9.5 - 单文件架构恢复版
# 所有功能已完全内置到 torrent_maker.py 单文件中，无需外部依赖

# 可选：系统性能监控（用于高级性能统计）
psutil>=5.8.0

# 可选：更好的进度条显示（如果系统不支持 Unicode 字符）
# tqdm>=4.60.0

# 可选：更精确的文件哈希计算（对于大文件）
# xxhash>=2.0.0

# 注意事项：
# 1. v1.9.5 已恢复真正的单文件架构，所有功能内置
# 2. 以上依赖都是完全可选的，程序无需任何外部依赖即可运行
# 3. 路径补全、进度监控、搜索历史等功能已全部内置
# 4. 如果要使用高级性能监控功能，建议安装 psutil